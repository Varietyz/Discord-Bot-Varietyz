/**
 * 📌 **Custom Modal Identifiers**
 *
 * This file centralizes all modal custom IDs, keeping them organized and preventing clutter in `interactionCreate.js`.
 */

const modalIds = ['register_clanchat_webhook_modal', 'open_register_clanchat_modal'];

module.exports = modalIds;
